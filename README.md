# abstract-sdk-graphql

- Create an [API token](https://app.goabstract.com/account/tokens). More info on https://sdk.goabstract.com/docs/authentication/
- Save your api token in `ABSTRACT_SDK_TOKEN` in `.env` file.